Release Notes Validation Report — Inji Verify 0.17.0
Generated by /crn-iv skill
======================================================

RESULT: PASS (0 errors, 3 warnings) — Markdown rendering proceeded.

--- Errors (blocking) ---
None.

--- Warnings (non-blocking) ---

⚠️ CON-009 [WARNING] supportResources.documentation: no `test_report` link.
   Reason: releaseType is Minor, and CON-009 recommends a test-report link for Major/Minor
   releases. No CI test-report artifact URL was discoverable from git history or PR evidence.
   Fix: add a `supportResources.documentation.test_report` URL once a human has the correct
   CI run/report link (e.g. Jenkins or GitHub Actions run for the v0.17.0 tag build).

⚠️ CON-012 [WARNING] userStories: section absent for a Minor release.
   Reason: This repo's evidence trail (git + GitHub PRs) does not contain a formal user-stories
   table; that artifact normally lives in JIRA, which was not queried as part of this run.
   Fix: pull the JIRA "0.17.0" fix-version issue list (project INJIVER) and add a
   content.userStories table (JIRA ID + description) if this document is meant to satisfy
   CON-012, or accept the release notes without it since this is a warning, not a blocker.

⚠️ Deprecation with unknown removal version (evidence gap, not a numbered rule violation)
   The @mosip/react-inji-verify-sdk → @injistack/react-inji-verify-sdk package rename (PR #1079,
   INJIVER-1520) is documented as an `enhancedFeatures` entry with migrationRequired=true rather
   than a formal `content.deprecations` entry, because no PR body or commit stated a removal
   version for the old package name. Filling that field with a guessed version would violate
   CON-013 (no placeholder/fabricated content). If a removal version is decided, move this entry
   to `content.deprecations` with the schema's required `removalVersion` field.

--- Rendering source note ---
The central rendering.yaml (fetched live from svahsek/intuiract at generation time) renders
bugFixes as narrative H3 blocks per-entry (not the column table shown as an example in this
repo's local .claude/skills/crn-iv/SKILL.md), and knownIssues as a 5-column table (Issue |
Description | Workaround | Fixed In | Tracker) rather than the 4-column example in SKILL.md.
Per the skill's own Step 1 instruction ("resolve all other URLs from the manifest — never
hardcode file paths"), the live central rendering.yaml was treated as authoritative for the
final Markdown, and the bugFixes section below uses its per-entry heading format rather than a
table. Flagging this so a human can confirm this is the intended precedence.

--- Context note ---
v0.17.0 was already tagged and released on 2026-03-04 (before this run, on 2026-07-09). This
generation was explicitly requested as an as-shipped documentation pass, not a pre-release
draft — confirmed with the requester before proceeding, since docs/releases/release-inputs.yaml
originally pointed at a release_ref ("release-v0.17.0" / "release-0.17.0", duplicated key) that
did not resolve to any branch or tag in the repository.

--- Evidence coverage ---
134 non-merge commits / 133 PRs in v0.16.0..v0.17.0 examined (commit subjects + GitHub PR
title/body/labels; no PR labels are in use in this repository). ~95 commits classified as
internal (chore/ci/test/docs/build/infra-without-metric/reverts/version-bumps) and omitted per
classification-rules.yaml. See docs/releases/0.17.0-release-evidence.yaml for full source
mapping, confidence tiers, and the specific PR numbers backing each entry below.

Published sections: newFeatures (3), enhancedFeatures (2), bugFixes (17), securityUpdates (3),
technicalImprovements.infrastructure (4). knownIssues, deprecations, and apiChanges were left
out entirely (not populated) rather than included empty, per CON-004.
